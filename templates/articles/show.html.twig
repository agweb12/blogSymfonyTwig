{% extends 'base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block main %}
    <h1>Article n°{{ article.id }} de {{ app.user.firstName }}</h1>

    <a href="{{ path('app_my_articles') }}" class="btn btn-info">Voir mes articles</a>
    <a href="{{ path('app_articles_edit', {'id': article.id}) }}" class="btn btn-warning">Editer</a>
    {# <form method="post" action="{{ path('app_articles_delete', {'id': article.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">
        <button class="btn btn-danger mt-2 mb-3">Supprimer</button>
    </form> #}
    <table class="table">
        <tbody>
            <tr>
                <th>Title</th>
                <td>{{ article.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src="{{asset("images/articles/" ~ article.image)}}" class="card-img-top w-auto" alt="" height="100"></td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ article.content }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ article.createdAt ? article.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ article.updatedAt ? article.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('articles/_delete_form.html.twig') }}
{% endblock %}
